# cl-libheif

`cl-libheif` is a wrapper for `libheif` library for Common Lisp. It allows to
decode end encode HEIC images and read metadata blocks.

Read documentation [here](https://shamazmazum.github.io/cl-libheif/)
